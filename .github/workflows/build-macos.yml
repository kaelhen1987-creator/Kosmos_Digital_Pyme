name: Build macOS App

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
      - 'docs/**'

jobs:
  macos_app:
    name: Build macOS App
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install Dependencies
        run: |
          pip install flet
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Flet Build macOS
        run: |
          flet build macos --verbose --yes
      
      - name: Debug - List build output
        run: |
          echo "=== Listing build directory structure ==="
          if [ -d build ]; then
            find build -maxdepth 3 -print
          else
            echo "build directory not found"
          fi
          echo "=== Searching for .app bundles ==="
          find . -name "*.app" -type d 2>/dev/null || echo "No .app bundles found"

      - name: Create DMG (if app exists)
        run: |
          if [ -d "build/macos" ]; then
            cd build/macos
            APP_PATH=$(find . -name "*.app" -type d -maxdepth 1 | head -n 1)
            if [ -n "$APP_PATH" ]; then
              APP_NAME=$(basename "$APP_PATH")
              echo "Creating DMG for $APP_NAME"
              cd ../..
              mkdir -p dist
              hdiutil create -volname "Digital PyME" -srcfolder "build/macos/$APP_NAME" -ov -format UDZO "dist/Digital_PyME.dmg"
              echo "DMG created successfully"
            else
              echo "No .app bundle found in build/macos"
            fi
          else
            echo "build/macos directory not found"
          fi

      - name: Upload macOS DMG (Emergency Backup)
        run: |
          # Encontrar el archivo DMG
          DMG_PATH=$(find dist -name "*.dmg" | head -n 1)
          
          if [ -z "$DMG_PATH" ]; then
            echo "❌ No se encontró archivo DMG"
            exit 1
          fi
          
          echo "🚀 Archivo a subir: $DMG_PATH"
          DMG_SIZE=$(du -h "$DMG_PATH" | cut -f1)
          echo "📊 Tamaño: $DMG_SIZE"
          echo ""
          
          # OPCIÓN A: File.io (Rápido, link expira tras 1 descarga)
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Intentando subir a File.io..."
          curl -F "file=@$DMG_PATH" https://file.io > response.json
          echo ""
          echo "✅ LINK DE DESCARGA (FILE.IO):"
          cat response.json | grep -o '"link":"[^"]*"' | cut -d'"' -f4
          echo "⚠️  Este link expira tras 1 descarga"
          echo ""
          
          # OPCIÓN B: BashUpload (Respaldo, dura más tiempo)
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Subiendo a BashUpload (Respaldo)..."
          curl https://bashupload.com -T "$DMG_PATH"
          echo ""
          echo "✅ (Si file.io falló, usa el link de arriba)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
